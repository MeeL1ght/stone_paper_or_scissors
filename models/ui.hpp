#include <iostream>
#include <string>
// #include "./../utilities/stringUtils.cpp"

namespace UI {

  /* void create(std::string type) {
    type = StringUtils::toLowerCase(type);

    if (type == "int")
  } */
}
